<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zd.dao.DetailsDao">
  <select id="findSap" parameterType="string" resultType="com.zd.dto.SAPDto">
    SELECT s.title shop,p.title product,p.price,p.sale_num saleNum,d.coupon_value couponValue,d.url FROM shops s LEFT JOIN details d ON
s.shop_id=d.`shop_id` LEFT JOIN products p ON d.`product_id`=p.`product_id` WHERE s.title like #{sap}
  </select>
</mapper>